const statusChecker = () => {
    
}

module.exports = {
 data : new SlashCommandBuilder()
    .setName("status")
    .setDescription("Find the status of the service"),
    async execute(interaction) {
    
    } 
}